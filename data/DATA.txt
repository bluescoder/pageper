Data folder stores dynamic data for the application. 
In here are included plugins, data generated by the application and so on. When a version update is done 
this folder stays inmutable while only application folder is changed.

  * private : contians dynamic private data. In there a folder called plugin stores all external plugins added to the application.
  * public : contains data exposed to public access.
  
